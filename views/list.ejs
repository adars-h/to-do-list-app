<!DOCTYPE html>
<html lang="en" >
<head>
    <meta charset="utf-8">
	<title>To Do List</title>
    <link rel="stylesheet" href="css/style.css" type="text/css">
    <script src = "public/../index.js"></script>
</head>
<body>
    <form class="logout" action="/logout" method = "get">
        <input class="button-3" type="submit" value="Logout">
    </form>
    <div class = "box" id="heading">
        <h1><%= listTitle %></h1>
    </div>
     <div class="box" id="heading">
        <h1><%=Day %></h1>
     </div>

    <div class="box">
     <%for(var i=0;i<newItems.length;i++){ %>
         <form action="/tasks/delete" method="post">
            <div class="item">
                <input type="checkbox" name="checkbox" value="<%= newItems[i]._id %>" onChange="this.form.submit()">
                <p><%=newItems[i].name%></p>
            </div>
            <input type="hidden" name="listName" value="<%= listTitle %>"></input>
         </form>
     <%}%>
     <form class="item" action="/tasks" method="post" autocomplete="off">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value="<%= listTitle %>">+</button>
     </form>
     </div>
   
</body>
</html>